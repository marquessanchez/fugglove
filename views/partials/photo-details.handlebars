<section class="card">
  <a href="/photo/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="/images/{{filename}}" alt="{{description}}">
      <p>By: FUGG {{photo.artist}}</p>
    </div>
  </a>
</section>
